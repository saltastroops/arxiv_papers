[project]
name = "arxiv-papers"
version = "0.1.0"
description = "Export arXiv paper metadata as a spreadsheet"
requires-python = ">=3.11"
dependencies = [
    "arxiv>=2.2",
    "openpyxl>=3.1",
]

[dependency-groups]
dev = [
    "ruff>=0.11.5",
]
